---
name: typescript-strict-enforcer
description: Enforce strict, safe TypeScript practices across modules and boundaries. Use when the task is TypeScript-heavy implementation, typing cleanup, or preventing unsafe casts and implicit any usage.
---

# TypeScript Strict Enforcer

- Keep strict mode and null safety intact.
- Prefer sound types over assertions and escape hatches.
- Use explicit return types where they clarify contracts.
- Promote strong typing across API, service, and UI layers.
